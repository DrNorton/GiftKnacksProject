/*
Deployment script for GiftKnacks_db

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "GiftKnacks_db"
:setvar DefaultFilePrefix "GiftKnacks_db"
:setvar DefaultDataPath ""
:setvar DefaultLogPath ""

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
USE [$(DatabaseName)];


GO
/*
The column Text on table [dbo].[Comments] must be changed from NULL to NOT NULL. If the table contains data, the ALTER script may not work. To avoid this issue, you must add values to this column for all rows or mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column UpdateTime on table [dbo].[Comments] must be changed from NULL to NOT NULL. If the table contains data, the ALTER script may not work. To avoid this issue, you must add values to this column for all rows or mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column UserId on table [dbo].[Comments] must be changed from NULL to NOT NULL. If the table contains data, the ALTER script may not work. To avoid this issue, you must add values to this column for all rows or mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[Comments])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Dropping [dbo].[FK_Comments_ToUsers]...';


GO
ALTER TABLE [dbo].[Comments] DROP CONSTRAINT [FK_Comments_ToUsers];


GO
PRINT N'Dropping [dbo].[FK_WishLink]...';


GO
ALTER TABLE [dbo].[WishLinkComments] DROP CONSTRAINT [FK_WishLink];


GO
PRINT N'Altering [dbo].[Comments]...';


GO
ALTER TABLE [dbo].[Comments] ALTER COLUMN [Text] NVARCHAR (300) NOT NULL;

ALTER TABLE [dbo].[Comments] ALTER COLUMN [UpdateTime] DATETIME NOT NULL;

ALTER TABLE [dbo].[Comments] ALTER COLUMN [UserId] BIGINT NOT NULL;


GO
PRINT N'Creating [dbo].[FK_Comments_ToUsers]...';


GO
ALTER TABLE [dbo].[Comments] WITH NOCHECK
    ADD CONSTRAINT [FK_Comments_ToUsers] FOREIGN KEY ([UserId]) REFERENCES [dbo].[Users] ([Id]) ON DELETE CASCADE;


GO
PRINT N'Creating [dbo].[FK_WishLink]...';


GO
ALTER TABLE [dbo].[WishLinkComments] WITH NOCHECK
    ADD CONSTRAINT [FK_WishLink] FOREIGN KEY ([WishId]) REFERENCES [dbo].[Wishes] ([Id]);


GO
